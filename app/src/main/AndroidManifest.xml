<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.anameplease.fitlogalpha">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.anameplease.fitlogalpha.MainActivity" />
        </activity>
        <activity
            android:name=".MaxEstimateActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".FitUtilActivity" />
        <activity
            android:name=".NoteListActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LogCreator"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".TXTLogActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DBViewActivity"></activity>
    </application>

</manifest>